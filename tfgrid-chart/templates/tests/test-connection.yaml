apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tfgrid-chart.fullname" . }}-test-connection"
  labels:
    {{- include "tfgrid-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tfgrid-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
