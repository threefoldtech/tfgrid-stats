apiVersion: v1
kind: Service
metadata:
  name: {{ include "tfgrid-chart.fullname" . }}
  labels:
    {{- include "tfgrid-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tfgrid-chart.selectorLabels" . | nindent 4 }}
